TraceEnable off

RewriteEngine on
RewriteCond %{HTTP:X-Forwarded-Proto} !https
RewriteRule .* https://%{HTTP_HOST}%{REQUEST_URI} [R,L]

AuthUserFile /app/.htpasswd
AuthGroupFile /dev/null
AuthName "Enter"
AuthType Basic
require valid-user

SSLProxyEngine on
SSLProxyCheckPeerCN off
SSLProxyCheckPeerName off

ProxyRequests Off
ProxyPass / ${fwd}
ProxyPassReverse / ${fwd}
ProxyMaxForwards 10
